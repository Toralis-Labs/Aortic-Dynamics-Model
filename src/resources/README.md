# Geometry Segmentation Resources

This folder defines the rules for this repository.

This repository solves one problem only:

> Segment a vascular surface into an aortic body and anonymous connected branch segments using clean circular cut-boundaries.

The code should not solve vessel naming, clinical interpretation, measurements, device matching, simulation, or downstream workflow integration.

The only readable anatomical label allowed in outputs is:

```text
aortic_body